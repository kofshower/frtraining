import SwiftUI

struct LactatePRDV1View: View {
    var body: some View {
        ContentUnavailableView(
            "PRD 页面已清空",
            systemImage: "doc.text",
            description: Text("等待新的乳酸实验室 PRD UI 设计。")
        )
    }
}
